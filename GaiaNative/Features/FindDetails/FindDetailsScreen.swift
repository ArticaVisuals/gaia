// Legacy 3-tab Find Details screen.
// Keep this unavailable so the prototype path stays the only practical route.
import SwiftUI

@available(*, unavailable, message: "Use FindDetailsPrototypeScreen instead.")
struct FindDetailsScreen: View {
    let species: Species

    var body: some View {
        EmptyView()
    }
}
